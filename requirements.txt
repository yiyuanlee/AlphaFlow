# AlphaFlow Dependencies
# ========================
# Prefer: pip install -e ".[dev]"  (see pyproject.toml)
# This file is kept for backward compatibility with README quick-start.

# Core backtesting
backtrader>=1.9.78
yfinance>=0.2.0
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
pyyaml>=6.0

# Live trading (IBKR)
# Maintained continuation of ib_insync.
ib_async==2.1.0
exchange-calendars>=4.5,<5
pytz>=2024.0

# Development / testing
pytest>=7.0
pytest-asyncio>=0.21
tqdm>=4.0
